services:
  backend:
    build:
      context: .
      dockerfile: model_training/scripts/Dockerfile
    ports:
      - "8001:8001"
    environment:
      - OLLAMA_HOST=http://ollama:11434
    depends_on:
      - ollama
    volumes:
      - ./data:/app/data
      - ./model_training/vector_store:/app/model_training/vector_store
      - ./model_training/vector_store_examples:/app/model_training/vector_store_examples

  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama_storage:/root/.ollama

volumes:
  ollama_storage:

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "5173:5173" # Mapping VPS port 80 to container 5173
    depends_on:
      - backend
